{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "hooks": {
      "install": "npm install --legacy-peer-deps",
      "build": "npm run build"
    }
  }
}
